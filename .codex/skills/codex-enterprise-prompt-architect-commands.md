# Codex Enterprise Prompt Architect Commands

Compatibility shim.

The formal command reference lives here:

```text
.codex/skills/codex-enterprise-prompt-architect/commands.md
```

The formal skill entrypoint is:

```text
.codex/skills/codex-enterprise-prompt-architect/SKILL.md
```

Common requests:

```text
Codex için goal + full prompt üret: [görev]
```

```text
Goal kullanmadan plan-only Codex promptu üret: [görev]
```

```text
Yorum yapma, sadece prompt ver. Codex için prompt üret: [görev]
```

```text
Codex erken execute etti. STOP / RECOVER promptu üret.
```

For the full command list, use the formal `commands.md` file.
